<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.Esiea3a" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/Toolbar</item>
        <item name="colorPrimaryVariant">@color/widget</item>
        <item name="colorOnPrimary">@color/writeTool</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/fond</item>
        <item name="colorSecondaryVariant">@color/write</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="AppTheme" parent="Theme.Esiea3a">
        <!-- Customize your theme here. -->
        <!-- Base application thème. -->
        <item name="windowNoTitle">true</item>
        <item name="windowActionBar">false</item>
        <item name="spinnerDropDownItemStyle">@style/Widget.AppCompat.TextView.SpinnerItem</item>
    </style>

    <style name="Theme.Esiea3a.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="fonttext" parent="@android:style/TextAppearance.Small">
        <item name="android:fontFamily">@font/indieflowerregular</item>
    </style>
    <style name="fonttitle" parent="@android:style/TextAppearance.Small">
        <item name="android:fontFamily">@font/spiraxregular</item>
    </style>

    <style name="Theme.Esiea3a.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="Theme.Esiea3a.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
</resources>